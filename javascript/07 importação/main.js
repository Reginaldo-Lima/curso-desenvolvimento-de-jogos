
const {gets, print} = require('./funcoes-auxiliares');

print(gets());

/*

const funcoes = require('./funcoes-auxiliares');


console.log(funcoes);
*/

/*
// OBJECT DESTRUCTURING

const pessoa = {
    nome: 'Vitor'
}

const { nome } = pessoa;

// o código a cima é o mesmo que o de baixo.

const nome = pessoa.nome;

*/